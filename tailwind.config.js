module.exports = {
  darkMode: 'class', // Enables manual dark mode toggle via 'dark' class
  content: [
    './app/**/*.{ts,tsx}',
    './components/**/*.{ts,tsx}',
    './src/**/*.{ts,tsx}',
  ],
  theme: {
    extend: {
      // Optionally, extend the theme here (colors, spacing, etc.)
    },
  },
  plugins: [],
};